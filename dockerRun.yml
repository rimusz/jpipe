pipelines:
  - name: dockerRun
    steps:
      - name: runCommandsInDocker
        type: Bash
        configuration:
          runtime:
            type: image
              custom:
                image: alpine
                tag: latest
        execution:
          onExecute:
            - echo "I'm running in container!!!"
   
